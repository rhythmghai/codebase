# Core
fastapi
uvicorn
pydantic

# Orchestration
langgraph
langchain-core

# Retrieval / ML
scikit-learn          # TF-IDF stand-in embedder (sandbox-friendly, offline)
networkx               # graph representation (or swap for neo4j driver)

# --- Swap in for real deployment (outside this sandbox) ---
# sentence-transformers   # real embeddings (all-MiniLM-L6-v2) + cross-encoder reranker
# google-generativeai     # Gemini for query rewrite + generation (matches CareerRadar setup)
# psycopg2-binary         # Postgres client for Supabase deployment
# pgvector                # pgvector Python bindings
